# Core: built and tested against these versions
langchain-core>=1.5.4
langgraph>=1.2.11
pydantic>=2.13.4

# Needed for LIVE runs (not required for the scripted/fake-model test suite)
langchain-anthropic>=0.3.0

# --- Not yet in this project — Antigravity will add these when it builds
# the webhook + UI piece described in ANTIGRAVITY_PROMPT.md:
fastapi
uvicorn
httpx
pytest
python-dotenv
